# Saddleback Party Rentals — robots.txt
# https://www.saddlebackparty.com

User-agent: *
Allow: /

# Block common admin / system paths in case routes get added later
Disallow: /admin/
Disallow: /api/
Disallow: /.vercel/
# NOTE: /assets/ must stay crawlable — the homepage's render-critical JS/CSS live there.
# Blocking it makes Googlebot render the homepage as an empty shell.

# Sitemap
Sitemap: https://www.saddlebackparty.com/sitemap.xml
